[[source]]
name = "pypi"
url = "https://pypi.org/simple"
verify_ssl = true

[dev-packages]
pytest = "~=6.2"
pytest-cov = "~=2.11"

[packages]
pandas = "~=1.2"
plotly = "~=4.14"
dash = "~=1.19"
numpy = "~=1.20"

[requires]
python_version = "3.8"

[scripts]
test = "pytest --cov=./scripts -s -v"
test-coverage = "pytest --cov=./scripts --cov-report=xml -s -v"
dev = "python app.py"
